# Initial end-to-end benchmark harness smoke case.
# This case validates compilation, correctness, reporting, and cross-platform execution.
# It is not a performance claim because the workload is intentionally tiny.
name=arithmetic-smoke
warmup=3
samples=9
timeout_ms=2000
max_relative_mad=0.25
