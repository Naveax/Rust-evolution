# Runtime-dependent probe for input_int, repeat, and inferred mutability.
# CI records this with --report-only; acceptance requires IR inspection as well as timing.
name=runtime-repeat-v0
warmup=3
samples=11
timeout_ms=5000
max_relative_mad=0.15
